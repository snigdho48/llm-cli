name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version without v prefix (e.g. 1.0.0)"
        required: true
        default: "1.0.0"
      prerelease:
        description: "Mark GitHub Release as prerelease"
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Resolve version
        id: ver
        shell: pwsh
        run: |
          if ("${{ github.event_name }}" -eq "workflow_dispatch") {
            $version = "${{ inputs.version }}".Trim().TrimStart('v')
          }
          else {
            $tag = "${{ github.ref_name }}"
            if ($tag -notmatch '^v?\d+\.\d+\.\d+') {
              throw "Tag must look like v1.0.0 (got: $tag)"
            }
            $version = $tag.TrimStart('v')
          }
          "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          "tag=v$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          Write-Host "Resolved version=$version tag=v$version"

      - name: Build + package
        shell: pwsh
        run: ./scripts/release.ps1 -Version "${{ steps.ver.outputs.version }}"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: llm-cli-${{ steps.ver.outputs.version }}-win-x64
          path: |
            artifacts/release/llm-cli-${{ steps.ver.outputs.version }}-win-x64.zip
            artifacts/release/llm-cli-${{ steps.ver.outputs.version }}-win-x64.zip.sha256
            artifacts/release/llm-${{ steps.ver.outputs.version }}-win-x64.exe
            artifacts/release/llm-${{ steps.ver.outputs.version }}-win-x64.exe.sha256
            packaging/winget/manifest/
          if-no-files-found: error

      - name: Read SHA256
        id: sha
        shell: pwsh
        run: |
          $shaFile = "artifacts/release/llm-cli-${{ steps.ver.outputs.version }}-win-x64.zip.sha256"
          $line = Get-Content $shaFile -Raw
          $hash = ($line -split '\s+')[0].Trim()
          "sha256=$hash" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          $exeShaFile = "artifacts/release/llm-${{ steps.ver.outputs.version }}-win-x64.exe.sha256"
          $exeLine = Get-Content $exeShaFile -Raw
          $exeHash = ($exeLine -split '\s+')[0].Trim()
          "exe_sha256=$exeHash" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.ver.outputs.tag }}
          name: LLM CLI ${{ steps.ver.outputs.tag }}
          draft: false
          prerelease: ${{ github.event_name == 'workflow_dispatch' && inputs.prerelease }}
          generate_release_notes: true
          body: |
            ## LLM CLI ${{ steps.ver.outputs.tag }}

            Self-contained Windows x64 build — **no separate .NET install required**.

            ### Quick install (single EXE)

            1. Download `llm-${{ steps.ver.outputs.version }}-win-x64.exe`
            2. Rename to `llm.exe` (optional) and put it on your PATH, **or**
            3. Download the zip and run `install.ps1`

            ```powershell
            # Zip install
            Expand-Archive llm-cli-${{ steps.ver.outputs.version }}-win-x64.zip -DestinationPath .\llm-cli
            cd .\llm-cli
            .\install.ps1
            llm --help
            ```

            ### Checksums

            - `llm-${{ steps.ver.outputs.version }}-win-x64.exe` — SHA256: `${{ steps.sha.outputs.exe_sha256 }}`
            - `llm-cli-${{ steps.ver.outputs.version }}-win-x64.zip` — SHA256: `${{ steps.sha.outputs.sha256 }}`

            ### Winget

            Manifests for `Snigdho48.LLMCLI` are attached. Submit to winget-pkgs after this release is public.
          files: |
            artifacts/release/llm-${{ steps.ver.outputs.version }}-win-x64.exe
            artifacts/release/llm-${{ steps.ver.outputs.version }}-win-x64.exe.sha256
            artifacts/release/llm-cli-${{ steps.ver.outputs.version }}-win-x64.zip
            artifacts/release/llm-cli-${{ steps.ver.outputs.version }}-win-x64.zip.sha256
            packaging/winget/manifest/Snigdho48.LLMCLI.yaml
            packaging/winget/manifest/Snigdho48.LLMCLI.locale.en-US.yaml
            packaging/winget/manifest/Snigdho48.LLMCLI.installer.yaml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
